# 07.ls_object

`07.ls_object` は、`ls` コマンドをオブジェクト指向で実装していくための作業ディレクトリです。

## 現在の状態

現在、このディレクトリには `.gitkeep` のみが置かれています。
これは、実装ファイルがまだない状態でもディレクトリをリポジトリに含めておくためのファイルです。

## 今後このディレクトリに入る想定のもの

- `ls` の挙動を実装する Ruby ファイル
- 必要に応じたクラス分割用のファイル
- 動作確認やテストのためのファイル

今後、ここに `ls` のオブジェクト指向版の実装を追加していきます。
